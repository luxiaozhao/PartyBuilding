<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.partybuilding.partybuilding">
    <!-- android:icon="@mipmap/ic_launcher" -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/landing"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <!--android:theme="@style/AppTheme">-->


        <!-- android:theme="@style/Theme.AppCompat.NoActionBar"> -->
        <activity android:name=".start.GuideActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".home.activity.BaseWebActivity" />
        <!-- <activity android:name=".GuideActivity"></activity> -->
        <activity android:name=".MainActivity" />
        <activity android:name=".disembark.LoginActivity" />
        <activity android:name=".mine.activity.AboutUsActivity" />
        <activity android:name=".mine.activity.ChangePasswordActivity" />
        <activity android:name=".home.activity.SearchActivity"></activity>
    </application>

</manifest>